		GNET::Protocol *Clone() const {  return new DBSiegeGet(*this); }
	public:
		enum { PROTOCOL_TYPE = RPC_DBSIEGEGET };
		DBSiegeGet(Type type, Rpc::Data *argument, Rpc::Data *result)
			: RPC_BASECLASS(type, argument, result ) { }
		DBSiegeGet(const DBSiegeGet &rhs) : RPC_BASECLASS(rhs) { }
		int  PriorPolicy( ) const { return 1; }
		bool SizePolicy(size_t size) const { return size <= 10240; }
		bool TimePolicy(int timeout) const { return timeout <= 40; }
